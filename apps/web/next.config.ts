import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	reactStrictMode: true, // Checks that useEffect works correctly(by running it multiple times) and shows more warnings
};

export default nextConfig;
