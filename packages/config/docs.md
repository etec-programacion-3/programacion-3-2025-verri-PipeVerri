# Config Package Documentation

## Overview

The config package provides shared TypeScript configuration for the monorepo.

## Structure

- `tsconfig.base.json`: Base TypeScript configuration with strict settings and ES2023 target.

## Key Components

- **Shared Configuration**: Used by other packages and apps to maintain consistent TypeScript settings.